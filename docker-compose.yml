version: "3.5"
services:
  api:
     build: ./modules/restapi
     networks:
      - app-network
     ports:
      - "8080:8080"
  client:
     build: ./modules/frontend
     restart: unless-stopped
     ports:
      - "80:80"
     networks:
      - app-network
networks:
   app-network: